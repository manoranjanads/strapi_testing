{
  "routes": [
    {
      "method": "GET",
      "path": "/image-items",
      "handler": "image-item.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-items/count",
      "handler": "image-item.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-items/:id",
      "handler": "image-item.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/image-items",
      "handler": "image-item.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/image-items/:id",
      "handler": "image-item.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/image-items/:id",
      "handler": "image-item.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
